# -*- coding: utf-8 -*-
# vim: ft=yaml
---
elasticsearch:
  pkg:
    name: elasticsearch
  rootgroup: root
  config: '/etc/elasticsearch'
  service:
    name: elasticsearch
  subcomponent:
    config: '/etc/elasticsearch-subcomponent-formula.conf'
  # Just here for testing
  added_in_defaults: defaults_value
  winner: defaults
